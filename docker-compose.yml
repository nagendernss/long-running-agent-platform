services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hellocounsel
      POSTGRES_PASSWORD: hellocounsel
      POSTGRES_DB: hellocounsel
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
